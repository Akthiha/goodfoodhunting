const express = require ('express')

const router = express.Router ()

/*
router.get ('/users') //list of users
router.post ('/users') // create a user
router.delete ('/users/:id') // delete a user
router.post ('/users/:id') // update single user
router.post ('/users/new') // get new user form
router.post ('/users/:id/edit') // get existing user form
router.post ('/users/:id') // get single user
*/
